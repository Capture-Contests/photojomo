# System
.DS_Store
Thumbs.db

screenshots/

# Node
node_modules/
npm-debug.log
yarn-error.log

# Angular build output
dist/
.angular/

# Java / Maven
target/
*.class
*.jar

# Terraform
**/.terraform/
*.tfstate
*.tfstate.backup
*.tfvars

# IDE
.idea/
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json

# Secrets
.env
*.env.local

# Claude Code local settings (per-developer)
**/.claude/settings.local.json

# Cloudflare Wrangler local cache
.wrangler/
**/.wrangler/

# Local dev server logs
**/serve-*.log
**/serve-*.out.log
**/serve-*.err.log

# Local pre-merge snapshots (see capture-contests-wb_copySep3, made 2026-09-03)
*_copySep3/

# Capture Caribbean mailer — live credentials and stored enquiries never go in git
capture-contests-wb/server-php/cc-config.php
cc-enquiries/

# Video master: 75 MB, and only the encoded web renditions are ever served.
# Keeping it out of git also keeps it out of the Cloudflare build, which
# publishes everything under public/.
capture-contests-wb/public/video/FINAL - LOGO FIXED.mp4
