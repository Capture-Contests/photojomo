/**
 * Development environment. Point inquiryEndpoint at whatever you are testing
 * against — a local stub, a preview deployment, or the live endpoint.
 */
export const environment = {
  production: false,
  apiBaseUrl: '',
  inquiryEndpoint: '/api/inquiry',
};
